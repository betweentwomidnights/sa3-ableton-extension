# Small HTTP wrapper dependencies.
#
# Stable Audio 3 itself should be installed from the official upstream repo with
# uv so its own pyproject.toml remains the source of truth for model deps.
flask>=3.0
flask-cors>=6.0
python-dotenv>=1.0
soundfile>=0.13.1
